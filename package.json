{
  "name": "chatterbox",
  "scripts": {
    "start": "NODE_ENV=production concurrently \"cd server && npm run dev\" \"cd client &&npm start\"",
    "heroku-postbuild": "npm install && cd server && npm install && cd ../client && npm install --only-dev && npm install && npm run build"
  },
  "dependencies": {
    "concurrently": "^6.2.1"
  }
}
